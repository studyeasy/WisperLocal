# WisperLocal dependencies
faster-whisper>=1.0.0     # Whisper transcription engine (CTranslate2 backend)
sounddevice>=0.4.6        # Microphone capture
numpy>=1.24,<3            # Audio buffers
pynput>=1.7.6             # Global hotkey listener + synthetic Ctrl+V
pyperclip>=1.8.2          # Clipboard read/write for paste-at-cursor
PySide6>=6.6              # Settings window + system tray icon
huggingface_hub>=0.21     # Download/cache Whisper + LLM models (also via faster-whisper)

# Enhanced writing: in-process LLM (no server, no Ollama).
#
# IMPORTANT - build for broad CPU compatibility. The prebuilt CPU wheels target
# AVX2, which CRASHES (illegal instruction, 0xC000001D) on CPUs without AVX2.
# Build from source with AVX disabled so the app runs on ANY x86-64 CPU:
#
#   set CMAKE_ARGS=-DGGML_NATIVE=OFF -DGGML_AVX=OFF -DGGML_AVX2=OFF -DGGML_AVX512=OFF -DGGML_FMA=OFF -DGGML_F16C=OFF
#   set CMAKE_GENERATOR=Visual Studio 17 2022
#   pip install --no-binary llama-cpp-python "llama-cpp-python==0.3.30"
#
# (GPU builds - CUDA/Vulkan/Metal - are produced per-target in CI; the code
#  auto-offloads to the GPU when a GPU-capable build is installed.)
llama-cpp-python==0.3.30  # Runs quantized GGUF models locally (CPU; GPU via per-target build)
